# generate-poster

This is a small Bash script that extracts a still image (“poster frame”) from a video file or URL using `ffmpeg`.

`chmod +x generate_poster.sh`

`./generate_poster.sh`

## Parameters

1. video URL or local path
1. seconds or frame number
1. number of seconds or frames
1. set output path
1. set width
1. generate image
